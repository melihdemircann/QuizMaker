import 'package:flutter/material.dart';

import 'Pages/Log_In.dart';

void main() => runApp(
    MaterialApp(
      debugShowCheckedModeBanner: false,
      home: HomePage(), // Uygulama bu sayfayla başlayacak.
    )
);
